import styled from 'styled-components';

import { Card } from '@archie-webapps/shared/ui/design-system';

export const ErrorCardStyled = styled(Card)`
  .subtitle {
    margin-top: 1.5rem;
  }

  .error-image {
    margin: 2.5rem 0;
  }
`;
